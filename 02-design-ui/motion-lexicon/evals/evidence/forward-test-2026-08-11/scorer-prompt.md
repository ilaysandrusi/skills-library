You are the zero-context independent release scorer for Motion Lexicon v4.2.0. Score every preserved run against forward-test-contract.json and evals.json without relying on the task owner's provisional conclusions. Verify exact prompts, fresh HOME/CODEX_HOME/workdirs, exclusion of evals and neighboring runs, frozen Skill hashes, real builds, browser acceptance, bundled Blueprint validation, candidate structure, portable source, and every critical plus eval assertion. A run passes only when all critical assertions and at least 90 percent of all assertions pass. Record a passing suite only when all 36 independent runs are clean and pass. Do not commit, push, deploy, or fabricate evidence.

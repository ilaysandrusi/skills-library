# Troubleshooting

If VibeSkills is not working, delete `<SkillsDir>/vibe` and install the current release again.

If `vibe` does not appear after installation, make sure the AI application scans that `SkillsDir`.

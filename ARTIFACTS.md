# ארטיפקטים נלווים — agents / commands / rules / hooks

הספרייה הזאת התחילה כאוסף של **skills** בלבד. אבל הרבה מהריפוזיטוריז שהסקילים הגיעו מהם
מכילים עוד סוגי קבצים שהם חלק מאותו פרויקט: סוכני משנה, פקודות סלאש, חוקים תמידיים והוקים.
בסבב ההשלמה והתיקון חזרנו לכל מקור, סרקנו אותו במלואו, וייבאנו את מה שהיה חסר —
**בלי להמיר שום דבר לסקיל**.

## שני מקומות, לפי בעלות

הארגון הוא **skill-centric**: ארטיפקט שאפשר להוכיח שהוא שייך לסקיל אחד ספציפי יושב
**בתוך תיקיית הסקיל**. ארטיפקט שמשרת את כל הריפו או כמה סקילים במקביל נשאר בעץ המשותף.

### 1. בתוך הסקיל — `<category>/<skill>/`

הסקיל הוא חבילה עצמאית: `SKILL.md`, `SOURCE.md`, ואז לפי הצורך `agents/`, `commands/`,
`rules/`, `hooks/`, `references/`, `scripts/`. אפשר להעתיק תיקיית סקיל אחת ולקבל את
כל מה שהיא צריכה. ה-`SOURCE.md` של הסקיל מפרט לכל תיקייה כזאת את **הנתיב במעלה הזרם
וההוכחה לבעלות**.

### 2. משותף — `agents/` · `commands/` · `rules/` · `hooks/`

| תיקייה | מה יש שם | למה זה שונה מסקיל |
|---|---|---|
| [`agents/`](./agents/) | הגדרות סוכני משנה | הסוכן הראשי **מאציל** משימה לסוכן משנה |
| [`commands/`](./commands/) | פקודות סלאש | מופעל **במפורש** על ידי המשתמש (`/review`) |
| [`rules/`](./rules/) | חוקים תמידיים | **תמיד טעון**, לא נבחר לפי הקשר |
| [`hooks/`](./hooks/) | הוקים של זמן ריצה | **רץ אוטומטית** באירוע (session start, לפני/אחרי כלי) |
| `NN-*/` | סקילים | נטען **לפי דרישה** כשההקשר מתאים |

בכל תיקיית מקור (`agents/<slug>/`, `commands/<slug>/`, …) יש `SOURCE.md` עם הריפו המקורי,
הנתיב במעלה הזרם, ה-commit שננעל בזמן הייבוא, והרישיון.

## למה משהו נשאר משותף

זאת ההחלטה הכי חשובה כאן, והכלל היה: **אין ניחושים**. ארטיפקט עובר לתוך סקיל רק אם מבנה
מעלה הזרם מוכיח בעלות. שלוש דרכי הוכחה שהתקבלו:

1. **תיקיית פלאגין עם סקיל אחד בדיוק** — למשל `plugins/gh-cli/` אצל trailofbits מכיל
   `skills/gh-cli/` יחיד ולידו `hooks/`. ההוקים לא יכולים לשרת שום דבר אחר.
2. **ריפו שמפרסם סקיל אחד בדיוק** — למשל `mvanhorn/last30days-skill`. ה-`hooks/` ברמת
   הריפו הוא ההוקים של הסקיל הזה.
3. **כל הסקילים במעלה הזרם התאחדו לסקיל מקומי אחד** — למשל `uucz/moyu` פרסם 7 וריאנטים
   של moyu שאוחדו כאן לסקיל אחד, ולכן ה-`commands/` וה-`rules/` שלו שייכים לו.

מה שנשאר משותף נשאר משתי סיבות:

- **משרת כמה סקילים** — `ecc` (285 סקילים), `context-engineering-kit`, `vibe-skills`,
  `addyosmani`, `cloudflare`, `microsoft` וכן הלאה. גם `agents/trailofbits/trailmark`:
  הפלאגין `plugins/trailmark/` במעלה הזרם מכיל 14 סקילים, והסוכנים שלו ברמת הפלאגין
  משרתים את כולם. העברה לסקיל אחד מהערימה הייתה בחירה שרירותית, והכפלה לכל הסקילים
  הייתה יוצרת עותקים מיותרים.
- **אין סקיל מקבל** — `commands/trailofbits/insecure-defaults`: לפלאגין הזה במעלה הזרם
  אין `SKILL.md` בכלל, ולכן אין לאן להעביר.

## אזהרה על hooks

הוקים הם **קוד שרץ אוטומטית** על ידי סוכן קוד. תקרא הוק לפני שאתה מחבר אותו.
במיוחד `08-context-engineering/zero/hooks/ensure-runner.sh` — הוא מוריד Node ומתקין
CLI של npm לתיקיית הבית שלך.

## ארטיפקטים בתוך סקילים — הסך הכול

זה כולל גם מה שירד ככה ממעלה הזרם מההתחלה, וגם מה שהועבר לכאן בארגון הזה:

| סוג | תיקיות | קבצים |
|---|---|---|
| rules | 37 | 475 |
| agents | 390 | 455 |
| commands | 22 | 101 |
| hooks | 24 | 86 |

הגדולות שבהן: `05-development/vercel-react-best-practices/rules` (72),
`16-ai-apis-media/vercel-react-best-practices--charlie85270/rules` (57),
`05-development/vercel-react-native-skills/rules` (38),
`25-data-databases/clickhouse-best-practices/rules` (33),
`15-integrations/composio/rules` (31).

## מקורות שנסרקו

מפת השיוך המלאה — איזה סקיל הגיע מאיזה ריפו — נמצאת ב-[`SOURCES.json`](./SOURCES.json).

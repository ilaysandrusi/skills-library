interface:
  display_name: "Suede Programmatic SEO"
  short_description: "Suede quality-gated search page systems"
  default_prompt: "Use $suede-programmatic-seo on [target]. The user wants many templated pages targeting keyword, location, integration, or comparison patterns. Work through template design, data sourcing, index-worthiness thresholds, and internal linking that holds up, ground every recommendation in evidence the user can check, and return the decisions, the reasoning, and what to measure next."
policy:
  allow_implicit_invocation: true

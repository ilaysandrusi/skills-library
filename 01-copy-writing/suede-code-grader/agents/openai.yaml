interface:
  display_name: "Suede Code Grader"
  short_description: "A-F ship verdict across 7 grading lanes"
  default_prompt: "Use $suede-code-grader on [diff/PR/branch/files]. Check instant-F triggers first, then grade all 7 lanes with evidence. Apply surface-type grade caps (auth, payment, migration). Return the overall grade, why it landed there, required upgrades, and ship gate verdict."
policy:
  allow_implicit_invocation: true

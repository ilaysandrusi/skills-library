# Handoff — <topic> (<YYYY-MM-DD>)

> A handoff is the note this session leaves for the next one. The agent writes it at session
> close (via `/close-session`); the SessionStart hook injects the NEWEST handoff automatically —
> so tomorrow's session opens already knowing where you left off. One file per session close,
> named `<topic>-<YYYY-MM-DD>.md`. Old handoffs stay as searchable history.

## 1. What was done
<what shipped / changed / got decided this session — short bullets, date-tagged facts>

## 2. Where things stand
<current state per active project: what's ready, what's blocked, what's waiting on whom>

## 3. Memory updates
<patterns saved to MEMORY.md · concepts written/updated in knowledge/ · rules proposed>

## 4. Next steps
<the ordered short list for the next session — most important first>

## 5. [Opening note for next session]
<one paragraph the next session should act on immediately: "start with X because Y";
anything NOT to re-open; any external fact that must be re-checked before acting on it>
